// Compatibility shim for older local checkouts that import `config.js`.
// New code imports the tracked `config-loader.js` directly, so copying this
// file is no longer required.
export * from './config-loader.js';
export { default } from './config-loader.js';
